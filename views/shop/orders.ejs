    <%- include('../includes/head.ejs') %>
</head>
<body>
    <%- include('../includes/navigation.ejs')%>
        <% if(orders.length>0){ %>
            <ul>
                <% orders.forEach(order=>{ %>
                <h1>#<%=order._id%></h1>
                <ul>
                    <% order.items.forEach(product=>{console.log(product) %>
                        <li><%= product.title %> (<%= product.quantity %>)</li>
                <% }); %>
                </ul>
        <% }); %>

            </ul>
        <%}else{%>
            <h1>No Orders</h1>
        <% } %>
<%- include('../includes/end.ejs') %>
